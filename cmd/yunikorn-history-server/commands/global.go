package commands

var (
	// ConfigFile is the path to the configuration file
	ConfigFile string
	// MigrationsDir is the path to the directory containing the database migrations
	MigrationsDir = "migrations"
)
